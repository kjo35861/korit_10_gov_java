package study.ch16;

public class memo {
    /**
     * static = "클래스에 소속된"
     *
     * 인스턴스 멤버 = 각 개체마다 별도로 존재
     * ex) Dog d1 = new Dog();
     *
     * static 멤버(클래스) = 클래스에 단 1개만 존재, 모든 객체가 공유
     *
     */
}
